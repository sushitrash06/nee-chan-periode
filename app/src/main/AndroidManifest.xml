<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.nee_chanpriode">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".UpdateActivity"></activity>
        <activity
            android:name=".navigasi"
            android:label="@string/title_activity_navigasi"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".DataHaidActivity"></activity>
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity"></activity>
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".ActivityCalendar"></activity>
        <activity android:name=".CalendarLib.CalendarActivity"></activity>
    </application>

</manifest>